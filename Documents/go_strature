

---

## 📁 ساختار استاندارد پروژه Go

در پروژه‌های حرفه‌ای Go معمولاً از ساختاری مثل این استفاده میشه:

```
myapp/
├── cmd/
│   ├── myapp/
│   │   └── main.go
│   └── worker/
│       └── main.go
├── internal/
│   ├── database/
│   │   └── connection.go
│   ├── service/
│   │   └── user_service.go
│   └── utils/
│       └── validator.go
├── pkg/
│   └── logger/
│       └── logger.go
├── api/
│   └── handler.go
├── go.mod
└── go.sum
```

حالا هرکدوم از این پوشه‌ها رو توضیح می‌دم 👇

---

### 🧠 1. `cmd/`

پوشه‌ی **اصلی برای entry points برنامه‌ها** است.

هر **برنامه اجرایی (binary)** درون یک زیرپوشه‌ی جداگانه داخل `cmd` قرار می‌گیره.

**مثال:**

* `cmd/myapp/` → فایل `main.go` برای سرویس اصلی API
* `cmd/worker/` → فایل `main.go` برای job worker یا cron job

✅ نکته:
درون هر پوشه `cmd` فقط باید کدهای شروع (bootstrap) باشن و logic اصلی در پکیج‌های دیگر قرار بگیره.

---

### 🔒 2. `internal/`

اینجا کدهایی قرار می‌گیرن که فقط برای **داخل پروژه** قابل import هستن (private package).

Go این ویژگی رو enforce می‌کنه:
اگر یه پکیج داخل `internal` باشه، فقط کدهای داخل همین پروژه می‌تونن اون رو import کنن.

**مثال:**

```
internal/service/user_service.go
internal/db/connection.go
```

✅ استفاده برای:

* لایه‌های business logic
* ارتباط با دیتابیس
* helperها یا middlewareهای مخصوص پروژه

---

### 📦 3. `pkg/`

کدهایی که می‌خوای **قابل استفاده مجدد** در پروژه‌های دیگه باشن.

مثلاً اگر logger یا auth middleware عمومی داری که ممکنه در پروژه‌های دیگه هم استفاده بشه، اون رو در `pkg` می‌ذاری.

**مثال:**

```
pkg/logger/
pkg/config/
pkg/auth/
```

✅ این پکیج‌ها “public” هستن — یعنی هر پروژه‌ی دیگه‌ای می‌تونه با import کردنشون ازشون استفاده کنه.

---

### 🌐 4. `api/`

گاهی این پوشه برای کدهای مربوط به API (تعاریف request/response، routeها یا protoها در gRPC) استفاده میشه.

**مثال:**

```
api/handlers/
api/routes.go
api/models.go
```

---

### 🧩 5. سایر پوشه‌های معمول

| پوشه       | کاربرد                           |
| ---------- | -------------------------------- |
| `configs/` | فایل‌های config مثل YAML یا JSON |
| `scripts/` | اسکریپت‌های build یا deployment  |
| `build/`   | Dockerfile، فایل‌های CI/CD       |
| `docs/`    | مستندات پروژه                    |
| `test/`    | تست‌های integration یا e2e       |

---

## 💡 جمع‌بندی

| پوشه                                      | توضیح                                       |
| ----------------------------------------- | ------------------------------------------- |
| `cmd/`                                    | نقطه شروع برنامه (main.go برای هر سرویس)    |
| `internal/`                               | کدهای خصوصی پروژه (غیرقابل import از بیرون) |
| `pkg/`                                    | کدهای عمومی و قابل اشتراک                   |
| `api/`                                    | لایه‌ی API، routeها، مدل‌ها، protoها        |
| `configs/`, `scripts/`, `build/`, `docs/` | بخش‌های جانبی و کمکی                        |

---

اگر خواستی، می‌تونم یه مثال واقعی از پروژه‌ای که با این ساختار نوشته شده (مثلاً یه mini REST API با `cmd`, `internal`, `pkg`) برات بسازم تا کاملاً در عمل ببینی.
آیا برات درستش کنم؟

